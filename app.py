# app.py
# Minimal starter so we can get Streamlit + repo wiring working.
# Once this runs on Streamlit Cloud, we’ll replace it with the full app code.

import streamlit as st

st.set_page_config(page_title="MLB DFS Playbook", page_icon="⚾", layout="wide")

st.title("MLB DFS Playbook — Starter")
st.write("If you see this on Streamlit, the app is wired correctly. Next step: paste the full app code.")